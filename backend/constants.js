const minContentLen = 10;
const maxContentLen = 500;
const minCommentLen = 5;
const maxCommentLen = 100;

module.exports = {
    minContentLen,
    maxContentLen,
    minCommentLen,
    maxCommentLen
}